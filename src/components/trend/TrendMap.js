import React, { Component } from 'react'
import { Layout, List } from 'antd'

const { Content } = Layout;

class TrendMap extends Component {
    state = {}
    render() {
        return (
            <Content style={{ padding: '0 100px', borderTop: '1px solid #ddd' }}>
                <div style={{ background: '#fff', padding: 24, height: '100vh' }}>
                    XXXXXXXXX
                    </div>
            </Content>
        );
    }
}

export default TrendMap;